# VZGLYD Registry Staging

This directory stages the contents intended for the separate `vzglyd/registry` repository.

The registry is a static JSON index of slide metadata and release URLs. Once the dedicated repo exists, copy these files there and populate `index.json` with real release artifacts.

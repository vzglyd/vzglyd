# Contributing to the VZGLYD Registry

To add a slide:

1. Publish the slide in a public repository.
2. Attach a `.vzglyd` file to a tagged release.
3. Add the slide entry to `index.json`.
4. Open a pull request.

Required fields include slide name, description, repository URL, ABI version, and one or more release download URLs with checksums.
